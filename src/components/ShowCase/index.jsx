import ProductCards from "../ProductCards";

import "./style.css"

const ShowCase = () => {
  return (
    <div className="showCaseContainer">
      <ProductCards />
    </div>
  );
};

export default ShowCase;
